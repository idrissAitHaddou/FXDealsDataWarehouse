# FXDealsDataWarehouse
FXDeals Data Warehouse,  A Spring Boot application that imports and validates foreign exchange (FX) deals, prevents duplicates, and stores valid records in a PostgreSQL database. Includes Docker setup, logging, validation, and clean layered architecture (Controller, Service, Repository).
